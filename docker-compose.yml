version: '3.8'

services:
  pdf-to-markdown:
    build: ./pdf_to_markdown_service
    container_name: pdf-to-markdown-service
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
